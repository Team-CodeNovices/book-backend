<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.book.dao.OurBookMapper">
    <insert id="insert" parameterType="java.util.List">
        INSERT INTO OurBook (bookname, author, publisher, genre, bookdetail , price, writedate) VALUES
        <foreach collection="list" item="item" separator="," >
            (
            #{item.bookname},
            #{item.author},
            #{item.publisher},
            #{item.genre},
            #{item.bookdetail},
            #{item.price},
            #{item.writedate}
            )
        </foreach>
    </insert>

    <select id="select" resultType="OurBookDto">
        select * from ourBook
    </select>


    <select id="selectnull" resultType="OurBookDto">
        SELECT *
        FROM ourBook
        WHERE bookdetail IS NULL AND bookname != '19세 도서';
    </select>

    <select id="keywordnull" resultType="OurBookDto">
        select * from ourBook where mainkeyword is null
    </select>

    <update id="updateBooksByList" parameterType="java.util.List">
        <foreach collection="list" item="book" separator=";"  >
            UPDATE ourBook
            SET
            link = #{book.link},
            image = #{book.image},
            author = #{book.author},
            publisher = #{book.publisher},
            genre = #{book.genre},
            contents = #{book.contents},
            bookdetail = #{book.bookdetail},
            authordetail = #{book.authordetail},
            price = #{book.price},
            writedate = #{book.writedate},
            mainkeyword = #{book.mainkeyword}
            WHERE bookname = #{book.bookname}
        </foreach>
    </update>


    <update id="updateMainKeyword" parameterType="java.util.List">
        <foreach collection="list" item="book" separator=";"  >
            UPDATE ourBook
            SET
            mainkeyword = #{book.mainkeyword}
            WHERE bookname = #{book.bookname}
        </foreach>
    </update>

    <select id="searchKeyword" resultType="OurBookDto">
        SELECT * FROM ourBook WHERE bookdetail LIKE CONCAT('%', #{keyword}, '%')
                                 OR genre LIKE CONCAT('%', #{keyword}, '%')
                                 OR bookname LIKE CONCAT('%', #{keyword}, '%')
    </select>
    
    <select id="bookdetailinfo" resultType="OurBookDto">
        select *  from ourBook Where bookname = #{bookname}
    </select>

</mapper>